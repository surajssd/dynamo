## Conversation
P3 on PR #14965 (CodeRabbit finding confirmed by dmitry-tokarev-nv): coalescing stores with mixed `shared_cache_eligible` collapsed the merged event to ineligible, losing provenance for the eligible prefix. Reviewer cautioned to apply the compatible_data and merge_data changes together because a debug_assert alone would panic on the exact case it describes.

## Actions
Added `pending.shared_cache_eligible == next.shared_cache_eligible` to `compatible_data`, removed the `&=` from `merge_data`, replaced the test that pinned the AND semantics with one asserting mixed chains flush as two chained events (parent_hash of the second equals the first block's hash). Ran `cargo test -p dynamo-llm --no-default-features --lib -- kv_router::publisher::batching`.

## Errors & Mistakes
None.

## Dead Ends
None.

## Hints for Future Agents
The coalescer emits at most two outputs per push; a mixed pair produces the flushed first store on the second push and the second store on flush. No other test or doc pinned the old merge behavior.

## Codebase Discoveries
lib/llm/src/kv_router/publisher/AGENTS.md: KV routing events are advisory; do not add fail-closed behavior to the ordinary pipeline.

## Open Questions
None.
