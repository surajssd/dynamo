## Conversation
Third P2 on PR #14965: all three parametrized cases of test_mooncake_optional_json_metadata_preserves_serving_capabilities failed because the Python `get_engine_specific` only decodes string values and both keys hold a JSON object or boolean. Reviewer asked to read through the `runtime_data` getter.

## Actions
Confirmed in lib/bindings/python/rust/llm/local_model.rs that `get_engine_specific` returns `PyResult<Option<String>>` via `get_engine_specific::<String>` and `runtime_data` returns `{key: value.to_string()}`. Rewrote the assertions to `json.loads(metadata.runtime_data[key])`. Built the bindings locally (uv venv .venv, maturin develop --uv with CARGO_TARGET_DIR pointing at the workspace target), installed the package editable plus the pytest plugins the pyproject filterwarnings reference. Confirmed the old assertions reproduce `invalid type: map, expected a string` and `invalid type: boolean true, expected a string` (3 failed, 16 passed), and the new file passes 19/19. Also ran test_mooncake_store_runtime.py and test_kv_router_args.py (123 pass).

## Errors & Mistakes
First pytest run aborted with PytestConfigWarning because pyproject filterwarnings names pytest_benchmark; installed pytest-benchmark, pytest-timeout, pytest-rerunfailures, pytest-order, pytest-xdist from container/deps/requirements.test.txt pins.

## Dead Ends
None.

## Hints for Future Agents
- `.venv` at repo root is gitignored and now has the bindings installed; reuse it.
- The `_core.pyi` stub says `get_engine_specific -> Any | None` but the Rust impl only handles strings; that mismatch predates this PR and was left alone.
- maturin on macOS builds dynamo-llm with default-features off via the bindings Cargo.toml target cfg, so it works without CUDA/NIXL.

## Codebase Discoveries
None beyond the above.

## Open Questions
Whether the binding's `get_engine_specific` should return the raw JSON string for non-string values; not in scope for this PR.
